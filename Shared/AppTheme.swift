import SwiftUI

/// Custom palette so the app doesn't ship with the default blue look.
enum AppTheme {
    /// Warm violet used as the app-wide tint: prominent buttons, "mine"
    /// message bubbles, the send button, and focus rings.
    static let accent = Color(red: 0.52, green: 0.34, blue: 0.82)
}